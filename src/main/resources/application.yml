
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/invoiceDB
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

file:
  upload-dir: uploads

ocr:
  tessdata-path: ${TESSDATA_PATH:C:/Users/karthik.jogu/AppData/Local/Programs/Tesseract-OCR/tessdata}

anthropic:
  api-key: YOUR_API_KEY
  model: claude-sonnet-4-6
  base-url: https://api.anthropic.com

invoice:
  confidence-threshold: 0.90