
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/invoiceDB
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

file:
  upload-dir: uploads
